{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Phase 1 Analysis Output Schema",
  "description": "Schema for validating analyzer agent outputs (01-04)",
  "type": "object",
  "required": ["agent_name", "timestamp", "findings"],
  "properties": {
    "agent_name": {
      "type": "string",
      "enum": [
        "structure-architecture-analyzer",
        "tech-stack-dependencies-analyzer",
        "code-patterns-testing-analyzer",
        "data-flows-integrations-analyzer"
      ]
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "findings": {
      "type": "object",
      "additionalProperties": true
    },
    "needs_verification": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["item", "question"],
        "properties": {
          "item": {
            "type": "string",
            "description": "Short topic identifier (e.g., 'Test coverage thresholds')"
          },
          "question": {
            "type": "string",
            "description": "Clear, actionable question for the engineer to answer"
          },
          "reason": {
            "type": "string",
            "description": "Optional context about why this needs verification"
          }
        }
      },
      "maxItems": 3,
      "description": "Maximum 3 items needing verification"
    },
    "confidence": {
      "type": "string",
      "enum": ["high", "medium", "low"]
    }
  }
}
