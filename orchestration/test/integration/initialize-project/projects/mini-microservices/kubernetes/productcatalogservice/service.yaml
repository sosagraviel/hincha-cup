apiVersion: v1
kind: Service
metadata:
  name: productcatalogservice
spec:
  type: ClusterIP
  selector: { app: productcatalogservice }
  ports:
    - name: grpc
      port: 7000
      targetPort: 7000
