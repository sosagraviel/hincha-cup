# keycloak

Realm + theme baked into a custom Keycloak image. No source code — pure
infrastructure service.

Realm: `mini-monorepo`. Client: `mini-monorepo-backend` (confidential).
Dev admin: `admin / admin`.
