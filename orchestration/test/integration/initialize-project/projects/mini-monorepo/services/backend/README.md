# backend

NestJS 11 + TypeORM + Keycloak JWT. Single representative `users`
module + `auth` module showing the request shape.

## Run

```bash
pnpm dev          # ts-node-dev style watch
pnpm test         # unit tests
pnpm test:integration   # docker-compose-bound
```
