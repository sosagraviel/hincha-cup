import type { LanguageConfig } from '../types.js';

export const lua: LanguageConfig = {
  key: 'lua',
  displayName: 'Lua',
  extensions: ['lua'],
  manifests: [],
  lockFiles: [],
  isUtility: true,
};
