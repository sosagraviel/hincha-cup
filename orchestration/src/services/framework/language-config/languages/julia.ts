import type { LanguageConfig } from '../types.js';

export const julia: LanguageConfig = {
  key: 'julia',
  displayName: 'Julia',
  extensions: ['jl'],
  manifests: [],
  lockFiles: [],
  isUtility: true,
};
