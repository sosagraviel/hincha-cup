import type { LanguageConfig } from '../types.js';

export const r: LanguageConfig = {
  key: 'r',
  displayName: 'R',
  extensions: ['r', 'rmd'],
  manifests: [],
  lockFiles: [],
  isUtility: true,
};
