{
  "$schema": "https://claude.ai/schemas/settings.json",
  "autoApprovalPatterns": {
    "bash": ["**", ".claude/**"],
    "read": ["**", ".claude/**"]
  },
  "_comment": "Auto-approval patterns for AI Framework. Allows initialize-project and other skills to run utility scripts without asking for permission each time."
}
